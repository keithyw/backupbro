<head>
  <title>backupbro</title>
</head>

<body>
  <h1>fuck the bitches up their asses</h1>

  {{> loginButtons}}

  {{#if currentUser}}
  <form class="new-woman">
      <input type="text" name="name" placeholder="Add A Woman" />
  </form>
  {{/if}}

  <ul>
      {{#each women}}
      {{> woman}}
      {{/each}}
  </ul>
</body>

<template name="hello">
  <button>Click Me</button>
  <p>You've pressed the button {{counter}} times.</p>
</template>

<template name="woman">
    <li>
        {{#if currentUser}}
        <button class="delete">&times;</button>
        {{/if}}
        {{ name }}
    </li>
</template>